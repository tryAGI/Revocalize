# Revocalize.CLI

Command-line interface for the Revocalize SDK generated with AutoSDK.

## Installation

```bash
dotnet tool install --global Revocalize.CLI --prerelease
```

## Usage

```bash
revocalize --help
revocalize api --help
```